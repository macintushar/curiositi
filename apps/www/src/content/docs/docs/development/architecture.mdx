---
title: 🧱 Architecture
description: System architecture and technical overview of Curiositi
---

Curiositi is a monorepo built with Turborepo and Bun. This document explains the system architecture, data flow, and technical decisions.

## High-Level Architecture

```mermaid
flowchart TB
    subgraph Client["Client Layer"]
        Browser["Browser<br/>(React 19)"]
    end

    subgraph App["Application Layer"]
        Platform["Platform<br/>(TanStack Start)"]
        Worker["Worker<br/>(Hono on Bun)"]
        Agent["Agent Module<br/>(AI SDK + MCP)"]
        
        Platform -->|"QStash / bunqueue"| Worker
        Platform -->|"Chat/Tool Calls"| Agent
    end

    subgraph Data["Data Layer"]
        Postgres["PostgreSQL<br/>(Drizzle ORM)"]
        S3["S3 Storage<br/>(Files)"]
    end

    Browser --"HTTPS"--> Platform
    Platform --> Postgres
    Platform --> S3
    Worker --> S3
    Worker --> Postgres
```

## Component Overview

### Platform (Web Application)

**Technology Stack:**
- **Framework:** React 19 + TanStack Start
- **Routing:** TanStack Router (file-based)
- **Data Fetching:** TanStack Query + tRPC v11
- **API:** tRPC for type-safe client-server communication
- **Styling:** Tailwind CSS v4 + shadcn/ui
- **Build Tool:** Vite 7 with Nitro for SSR
- **Auth:** Better Auth (Google OAuth + email/password)
- **Monitoring:** Sentry

**Key Directories:**
```
apps/platform/src/
├── routes/           # TanStack Router route definitions
├── components/       # React components
│   └── ui/          # shadcn/ui components
├── pages/           # Page-level views
├── layouts/         # Layout components
├── integrations/    # tRPC client and server setup
├── hooks/           # React hooks
├── lib/             # Utilities (auth, upload, etc.)
├── middleware/      # Server middleware
└── env.ts           # Environment variable validation
```

### Worker (File Processing)

The worker is a Hono server running on Bun that processes uploaded files. It operates in two modes depending on the queue provider:

- **QStash mode**: Runs as an HTTP server on port 3040. QStash sends signed requests to `POST /process-file`, and the worker verifies the signature before processing.
- **bunqueue mode**: Starts a bunqueue worker that polls the local bunqueue server for jobs. Also runs the HTTP server for health checks.

It exposes the following endpoints:

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/` | GET | Root health check |
| `/health` | GET | Health status endpoint |
| `/process-file` | POST | File processing endpoint (invoked by QStash/bunqueue) |

**Technology Stack:**
- **Framework:** Hono
- **Runtime:** Bun (port 3040)
- **AI:** Vercel AI SDK (`ai` package) with OpenAI and Google providers
- **Queue:** Upstash QStash (production) or bunqueue (local development)
- **Security:** QStash request signature verification for production

**Key Directories:**
```
apps/worker/src/
├── processors/      # File type processors
│   ├── doc.ts      # Document processor (PDF, text, etc.)
│   ├── image.ts    # Image processor (JPEG, PNG, etc.)
│   ├── word.ts     # Word document processor (.docx, .doc)
│   ├── excel.ts    # Excel spreadsheet processor (.xlsx, .xls)
│   ├── powerpoint.ts # PowerPoint processor (.pptx, .ppt)
│   └── types.ts    # Processor type definitions
├── lib/
│   ├── chunk.ts           # Text chunking (300 tokens, 60 overlap)
│   ├── embedding-context.ts # Context prefix builder for embeddings
│   ├── file-type-utils.ts # CSV detection and header extraction
│   └── md.ts              # Markdown utilities
├── process-file.ts # Main file processing logic
├── create-logger.ts # Per-request logger factory
├── sentry.ts       # Sentry error tracking integration
├── utils.ts        # Response helpers
├── index.ts        # Hono server entry point + bunqueue worker
└── env.ts          # Environment variable validation
```

### Database (PostgreSQL + pgvector)

**Technology Stack:**
- **Database:** PostgreSQL 14+ with pgvector extension
- **ORM:** Drizzle ORM
- **Migrations:** Drizzle Kit

**Key Tables:**
```
Better Auth Tables:
├── user
├── session
├── account
├── organization
├── member
├── verification
└── invitation

Curiositi Tables:
├── spaces               # Hierarchical spaces (parentSpaceId for nesting)
├── files                # File metadata (name, type, size, S3 key, status)
├── fileContents         # Extracted content chunks + vector embeddings (1536d)
├── filesInSpace         # Many-to-many junction table
├── agents               # Configurable AI agents
├── tools                # Built-in and MCP tools available to agents
├── agent_tools          # Junction for tools enabled on a specific agent
├── mcp_servers          # Connected Model Context Protocol servers
├── conversations        # Chat conversation metadata
├── messages             # Logs of user, system, and tool messages
├── organization_roles   # Workspace role permissions
└── organization_settings # Workspace-level key-value settings
```

### Shared Packages

```
packages/
├── agent/           # AI Agents and Model Context Protocol logic
│   └── src/
│       ├── create.ts        # Agent creation and streaming (runAgent)
│       ├── constants.ts     # Model configs, provider defaults
│       ├── types.ts         # Agent and tool type definitions
│       ├── system-agents.ts # System agent definitions (re-exported from share)
│       ├── prompts/         # Agent prompt templates
│       ├── mcp-server.ts    # MCP Client manager and routing
│       ├── mcp/             # MCP client implementation
│       │   ├── client-manager.ts  # MCP connection lifecycle
│       │   ├── client.ts          # MCP client
│       │   ├── discovery.ts       # MCP tool discovery
│       │   └── tool-adapter.ts   # MCP tool adapter
│       └── tools/           # Built-in tools
│           ├── file-search.ts      # Semantic file search
│           ├── web-search.ts       # Firecrawl web search
│           ├── webfetch-search.ts  # URL content fetcher
│           └── index.ts            # Tool registry and factory
├── db/              # Database schema and Drizzle config
│   └── src/
│       ├── schema.ts   # All table definitions
│       ├── client.ts   # Database client
│       └── index.ts    # Exports
├── share/           # Shared utilities
│   └── src/
│       ├── ai/         # AI model definitions (OpenAI, Google) and prompts
│       ├── constants/  # MIME types, file size limits, allowed types
│       ├── db-schemas/ # Zod validation schemas for space/file
│       ├── fs/         # File system helpers
│       ├── logger/     # Structured logging utility
│       ├── models/     # AI model catalog (bundled models.dev data)
│       ├── schemas/    # Shared Zod schemas (upload, auth, email)
│       └── types/      # Shared type definitions (queue, search, agents)
├── api-handlers/    # Shared API handler logic
│   └── src/
│       ├── upload.ts      # File upload handling
│       ├── file.ts        # File operations
│       ├── space.ts       # Space operations
│       ├── queue.ts       # Queue job dispatching
│       ├── invitation.ts  # Workspace invitation handling
│       └── response.ts    # Response helpers
├── email/          # Email package (Resend integration)
│   └── index.ts
├── queue/           # Job queue wrapper (supports QStash and bunqueue)
│   ├── index.ts          # Queue client factory
│   └── providers/
│       ├── qstash.ts     # Upstash QStash provider
│       └── bunqueue.ts   # Local bunqueue provider
└── tsconfig/        # Shared TypeScript configurations
```

## Data Flow

### File Upload Flow

```mermaid
flowchart LR
    subgraph Upload["1. User Upload"]
        Browser["Browser"] -->|"tRPC / api/upload"| Platform
        Platform -->|"File"| S3["S3 Storage"]
    end

    subgraph Store["2. Metadata Storage"]
        S3 -->|"File saved"| Platform
        Platform -->|"Save metadata"| DB[(PostgreSQL)]
        DB -->|"Status: pending"| Platform
        Platform -->|"Dispatch job"| Queue["QStash / bunqueue"]
    end

    subgraph Process["3. Background Processing"]
        Queue -->|"Invoke"| Worker["Worker"]
        Worker -->|"Download"| S3
        S3 -->|"File content"| Worker
        Worker -->|"Extract content"| Worker
        Worker -->|"Chunk text"| Worker
        Worker -->|"Generate embeddings"| Worker
        Worker -->|"Store"| DB
        Worker -->|"Status: completed"| DB
    end

    subgraph Ready["4. Search Ready"]
        DB -.->|"File searchable"| Search["Vector Search"]
    end
```

### Search Flow

```mermaid
flowchart TB
    subgraph Step1["1. Query Received"]
        User["User enters query"] --> Platform
    end

    subgraph Step2["2. Embedding Generation"]
        Platform -->|"Query text"| AI["Embedding Model"]
        AI -->|"Query vector"| Platform
    end

    subgraph Step3["3. Vector Search"]
        Platform -->|"Vector"| DB[(pgvector)]
        DB -->|"Top matching chunks"| Platform
    end

    subgraph Step4["4. Result Aggregation"]
        Platform -->|"Group by file"| Results["Results to user"]
    end
```

### Authentication Flow

```mermaid
flowchart TB
    Login["1. Login Request<br/>(Email/Password or Google)"] --> Validate["Better Auth validates"]
    Validate --> Session["2. Session Created<br/>(PostgreSQL + Cookie)"]
    Session --> Workspace["3. Workspace Context<br/>(Select workspace)"]
    Workspace --> Authz["4. Authorization<br/>(Check membership)"]
```

## Technical Decisions

### Why Turborepo?

- **Code Sharing** — Shared packages (db, share, api-handlers, queue) reduce duplication
- **Build Optimization** — Turborepo caches and parallelizes builds
- **Unified Tooling** — Single lint (Biome), format, and type-check across the repo
- **Dependency Management** — Workspace protocol for internal dependencies

### Why TanStack Start?

- **File-based Routing** — Automatic route generation via TanStack Router
- **Type Safety** — End-to-end TypeScript with tRPC integration
- **SSR Support** — Built-in server-side rendering via Vite + Nitro
- **Developer Experience** — Hot reload and excellent dev tools

### Why Hono for Worker?

- **Performance** — Lightweight and fast, runs natively on Bun
- **Simplicity** — Single endpoint, minimal framework overhead
- **TypeScript Native** — Full type safety out of the box

### Why Drizzle ORM?

- **Type Safety** — Full TypeScript support with inferred types
- **SQL-like Syntax** — Familiar query building, close to raw SQL
- **Migration Support** — Drizzle Kit for schema generation and migrations
- **Performance** — Minimal overhead compared to heavier ORMs

### Why pgvector?

- **Native Integration** — Vector storage directly in PostgreSQL
- **Similarity Search** — Efficient nearest neighbor queries
- **No Additional Services** — No need for a separate vector database
- **ACID Compliance** — Transactional safety for embeddings alongside relational data

## Monitoring and Observability

### Logging

- **Structured Logging** — Custom logger in `packages/share/src/logger/`
- **Log Levels** — Debug, Info, Warn, Error

### Error Tracking

- **Sentry** — Integrated into the platform for error tracking and performance monitoring

## CI/CD

### GitHub Actions Workflows

```mermaid
flowchart TB
    subgraph ci["ci.yml (push/PR to main)"]
        Format["Format check<br/>(Biome)"]
        Lint["Lint + Type Check<br/>(bun run check)"]
        Commit["Commit message<br/>(commitlint)"]
    end

    subgraph test["test.yml (push/PR to main)"]
        Test["Run tests<br/>(bun run test:coverage)"]
        Codecov["Upload coverage<br/>(Codecov)"]
    end

    subgraph release["release.yml (weekly, Friday 00:00 UTC)"]
        Changeset["Generate changesets"]
        Version["Version packages"]
        GitHub["Create releases/tags"]
    end
```

### Deployment

- **Platform** — Deploys to Vercel (TanStack Start + Nitro)
- **Documentation (www)** — Deploys to Vercel (Starlight + Astro)
- **Worker** — Runs on Bun

## Development

### Local Development

```bash
# Start all services
bun run dev

# Services available:
# - Platform: http://localhost:3030
# - Worker:   http://localhost:3040
```

### Testing

The project uses Bun's built-in test runner and Playwright for E2E tests:

```bash
# Run tests with coverage
bun run test

# Run with lcov coverage output
bun run test:coverage
```

## Next Steps

- [Configuration](/docs/development/configuration) — Environment variables and settings
- [Core Concepts](/docs/getting-started/core-concepts) — Understand the data model
- [Getting Started](/docs/getting-started/getting-started) — Run Curiositi locally
